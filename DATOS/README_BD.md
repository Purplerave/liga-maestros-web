# Base de datos activa

La web Liga de Maestros usa esta base:

`DATOS/LIGA_MAESTROS_PRO.db`

La ruta esta definida en `config.py` como `DB_PATH`. No usar bases `.db` sueltas en la raiz del proyecto.
